{
  "name": "aether-key-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*",
    "examples/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=@aether-key/react",
    "dev": "npm run dev --workspace=@aether-key/react",
    "dev:example": "npm run dev --workspace=basic-chat",
    "dev:aether-ollama": "npm run dev --workspace=aether-ollama",
    "proxy": "cd apps/python-proxy && uvicorn app.main:app --reload --port 8000",
    "proxy:docker": "cd apps/python-proxy && docker compose up --build"
  }
}
